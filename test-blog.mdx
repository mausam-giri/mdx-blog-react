---
title: 'Another Test Post'
date: '2023-04-28'
tags: ['next.js', 'nextjs', 'new']
---
We recommend using **Static Generation** whenever possible because yout page can be built once and served by CDN.

```ts
const sum = (num1: number, num2: number): number => {
  return num1 + num2
}
```
